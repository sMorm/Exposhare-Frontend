query userPosts($id: Int!, $context_id: Int!, $after: Int) {
  userPosts(id: $id, context_id: $context_id, after: $after) {
    id
    image_url
    liked
    created_at
    user {
      id
      username
      firstname
      lastname
      profile_picture
    }
  }
}