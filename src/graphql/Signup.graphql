mutation newUser($firstname: String!, $lastname: String!, $email: String!, $password: String!, $bio: String!, $username: String!) {
  newUser(firstname: $firstname, lastname: $lastname, email: $email, password: $password, bio: $bio, username: $username) {
    user {
      firstname
    }
    token
  }
}